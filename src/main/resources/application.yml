server:
  port: 8080
  context-path: /demo
  tomcat:
    uri-encoding: utf-8
logging:
  level: debug
spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
